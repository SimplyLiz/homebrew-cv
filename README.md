# homebrew-cv

Homebrew tap for [CognitiveVault CLI](https://github.com/SimplyLiz/cognitive-vault).

## Install

```bash
brew tap SimplyLiz/cv
brew install cv
```

## Usage

```bash
cv --version
cv login
cv vault list
```

Formulae in `Formula/` are auto-updated by goreleaser on each release.
